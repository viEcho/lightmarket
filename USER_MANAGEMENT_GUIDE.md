# 管理员用户管理功能使用指南

## 📋 功能概述

管理员登录后，可以进入"用户管理"页面，通过钱包地址查询用户，并向指定用户地址 Mock（铸造）USDC。

## 🎯 功能特性

### 1. 用户查询
- 输入完整的钱包地址（0x开头，42位）
- 实时查询链上 USDC 余额
- 显示用户地址和余额信息

### 2. Mock USDC
- 快速选择预设数量（100/1K/10K/100K USDC）
- 支持自定义数量
- 实时显示预计余额
- 交易状态实时反馈

## 🚀 使用步骤

### 步骤1：登录管理员账号

访问管理员登录页面：
```
http://localhost:5173/admin-login
```

输入管理员账号和密码登录。

### 步骤2：进入用户管理

登录后自动跳转到管理员仪表盘，点击顶部导航的 **"👥 User Management"** tab。

### 步骤3：搜索用户

1. 在搜索框中输入完整的钱包地址
   - 例如: `0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`
2. 点击 **"🔍 搜索"** 按钮或按 Enter 键
3. 查看搜索结果，显示用户地址和当前 USDC 余额

### 步骤4：Mock USDC

1. 在用户卡片中点击 **"🪙 Mock USDC"** 按钮
2. 弹窗显示：
   - 目标地址
   - 当前余额
3. 选择数量：
   - **方式1**: 点击快速选择按钮（100/1K/10K/100K）
   - **方式2**: 输入自定义数量
4. 查看预计余额
5. 点击 **"✅ 确认铸造"** 按钮
6. 在 MetaMask 中确认交易
7. 等待交易确认
8. 查看交易成功提示和新余额

## 📸 界面预览

### 用户管理页面

```
┌─────────────────────────────────────────────┐
│ 👥 用户管理                                  │
│ 查询用户并 Mock USDC                         │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ 输入用户钱包地址 (0x...)                    │
│ ┌────────────────────────────────┐  ┌───┐  │
│ │ 0xf39Fd6e51aad88F6F4ce6...    │  │搜索│  │
│ └────────────────────────────────┘  └───┘  │
│ 💡 输入完整的钱包地址进行搜索             │
└─────────────────────────────────────────────┘

搜索结果 (1)
┌─────────────────────────────────────────────┐
│ [0xF3]  0xf39Fd6e51aad88F6F4ce6a...         │
│        0xf39Fd6e51aad88F6F4ce6aB8827...      │
│        USDC 余额: 10000 USDC                │
│                                          [🪙 Mock] │
└─────────────────────────────────────────────┘
```

### Mock USDC 弹窗

```
┌──────────────────────────────────────────┐
│ 🪙 Mock USDC                         [✕] │
├──────────────────────────────────────────┤
│ 目标地址:                                 │
│ 0xf39Fd6e51aad88F6F4ce6aB8827279cff...    │
│                                          │
│ 当前余额: 10000 USDC                      │
│                                          │
│ 快速选择:                                 │
│ ┌─────┐ ┌──────┐ ┌───────┐ ┌────────┐ │
│ │ 100 │ │ 1,000│ │10,000 │ │100,000 │ │
│ └─────┘ └──────┘ └───────┘ └────────┘ │
│                                          │
│ 或输入自定义数量:                         │
│ ┌──────────────┐ [USDC]                 │
│ │   5000       │                         │
│ └──────────────┘                         │
│                                          │
│ 预计余额:                                 │
│ 15000 USDC                               │
├──────────────────────────────────────────┤
│            [✅ 确认铸造]  [取消]          │
└──────────────────────────────────────────┘
```

## 💡 使用技巧

### 快速查询测试用户

如果你在本地测试，可以使用 Hardhat 默认的测试地址：

```javascript
// Account 0 (默认)
0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266

// Account 1
0x70997970C51812dc3A010C7d01b50e0d17dc79C8

// Account 2
0x3C44CdDdB6a900fa2b585dd299e03d12FA4293BC
```

### 批量 Mock

如果需要给多个用户 Mock USDC：

1. 搜索第一个用户
2. Mock USDC
3. 清除搜索结果
4. 搜索下一个用户
5. 重复步骤 2-4

### 推荐数量

| 用途 | 推荐数量 | 说明 |
|------|---------|------|
| 小额测试 | 100 USDC | 测试小额交易 |
| 创建市场 | 1,000 USDC | 市场初始流动性 |
| 正常交易 | 10,000 USDC | 多次交易测试 |
| 大额测试 | 100,000 USDC | 压力测试 |

## 🔧 技术实现

### 前端组件

**文件**: `src/components/UserManagement.vue`

核心功能：
- 钱包地址验证（正则: `^0x[a-fA-F0-9]{40}$`）
- 链上余额查询（调用 MockUSDC 合约的 `balanceOf()`）
- Mock USDC（调用 MockUSDC 合约的 `mint()`）

### 合约交互

```javascript
// 查询余额
const balance = await usdcContract.balanceOf(userAddress)

// Mock USDC
const tx = await usdcContract.mint(userAddress, amountWei)
```

### MetaMask 集成

- 自动连接钱包
- 交易签名
- 交易状态跟踪
- Gas 费用支付

## ⚠️ 注意事项

### 1. 仅限测试网络

⚠️ **此功能仅用于测试网络！**

Mock USDC 的 `mint()` 函数没有权限控制，任何人都可以调用。

### 2. 钱包连接

使用此功能前，确保：
- MetaMask 已安装
- 钱包已连接到正确的网络（本地/测试网）
- 钱包中有足够的 ETH 支付 Gas 费

### 3. 地址格式

必须输入完整的钱包地址：
- 长度：42 个字符
- 格式：0x 开头 + 40 位十六进制
- 示例：`0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266`

### 4. Gas 费用

每次 Mock USDC 需要支付 Gas：
- 预估 Gas：~30,000
- 本地网络：免费
- 测试网：需要测试网 ETH

## 🐛 故障排除

### 问题1: "请输入有效的钱包地址"

**原因**: 地址格式不正确

**解决方案**:
1. 检查地址是否以 `0x` 开头
2. 检查地址长度是否为 42 个字符
3. 检查是否只包含十六进制字符（0-9, a-f, A-F）

### 问题2: 查询失败

**原因**:
- USDC 合约地址未配置
- 网络连接问题
- 合约未部署

**解决方案**:
```bash
# 检查 .env 配置
cat market-web/.env | grep USDC_ADDRESS

# 确保合约已部署
npx hardhat run scripts/deploy_usdc.js --network localhost
```

### 问题3: MetaMask 无法连接

**原因**:
- MetaMask 未安装
- 网络配置错误
- Chain ID 不匹配

**解决方案**:
1. 安装 MetaMask
2. 添加本地网络：
   - RPC URL: `http://localhost:8545`
   - Chain ID: `31337`
   - Currency Symbol: `ETH`

### 问题4: 交易失败

**原因**:
- Gas 不足
- 合约调用失败
- 网络拥堵

**解决方案**:
1. 检查钱包 ETH 余额
2. 增加 Gas limit
3. 稍后重试

## 📊 完整流程示例

### 场景：给测试用户 Mock 10,000 USDC

```bash
# 1. 启动本地节点
cd market-contract
npx hardhat node

# 2. 部署 USDC（新终端）
npx hardhat run scripts/deploy_usdc.js --network localhost
# 输出: MockUSDC deployed to: 0x5FbDB...

# 3. 更新前端配置
# market-web/.env
VITE_USDC_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3
VITE_CHAIN_ID=31337

# 4. 启动前端
cd market-web
npm run dev

# 5. 访问管理员页面
# http://localhost:5173/admin-login
# 登录后点击 "👥 User Management"

# 6. 搜索用户
# 输入: 0xf39Fd6e51aad88F6F4ce6aB8827279cffFb92266
# 点击搜索

# 7. Mock USDC
# 点击 "🪙 Mock USDC"
# 选择 "10,000"
# 点击 "✅ 确认铸造"

# 8. MetaMask 确认
# 查看交易详情
# 点击 "确认"

# 9. 等待交易确认
# 显示: ✅ 成功铸造 10000 USDC!

# 10. 验证余额
# 余额更新为: 10000 USDC
```

## 📚 相关文件

```
market-web/
├── src/
│   ├── components/
│   │   ├── UserManagement.vue     # 用户管理组件 ⭐
│   │   └── AdminDashboard.vue     # 管理员仪表盘（已集成）
│   └── utils/
│       └── faucet.js              # Mock USDC 工具函数
└── .env                           # 环境变量配置
```

## 🎉 完成！

现在管理员可以轻松地向任何用户地址 Mock USDC，方便进行测试和开发！
